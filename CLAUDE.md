# CLAUDE.md (WattLineWeb 프로젝트)

전주번호찰(선로 정보) 추출기. Vite + React 프론트엔드, Express/Vercel Serverless 백엔드(`api/extract.ts`, `server.ts`). 배포 대상: https://wattline-web.vercel.app/

## 1. 배포 전 반드시 문의할 것

- `vercel deploy`, `vercel --prod`, main 브랜치 push/merge 등 실제 배포로 이어지는 작업은 실행 전 사용자에게 반드시 확인받는다.
- 사용자가 사전에 "배포까지 알아서 해"처럼 명시적으로 포괄 승인한 경우가 아니면, 매번 새로 확인한다.

## 2. 반드시 한글로 답변할 것

- 사용자가 명시적으로 영문으로 요청하지 않는 한, 모든 대화·질문·답변은 한글로 한다.

## 3. 배포 완료 후 카카오톡 나챗으로 보고

- 배포가 완료되면 무엇을 배포했는지 요약해서 카카오톡 "나에게 채팅"(나챗)으로 완료 보고를 보낸다.
- 카카오톡 MemoChat 도구(`KakaotalkChat-MemoChat`)를 사용한다.
- 보고 내용에는 최소한 다음을 포함: 배포 시각, 변경 요약(1~3줄), 배포 URL/버전.

## 4. 방대하거나 장기 참고할 내용은 /docs 에 저장

- 세션 압축(컨텍스트 요약) 시 사라질 수 있는 내용, 또는 분량이 많아 대화창에 남기기 부적절한 내용(설계 근거, 조사 결과, 도메인 지식, 작업 로그 등)은 `/docs` 폴더에 `.md` 파일로 정리해서 남긴다.
- 파일명은 날짜 또는 주제를 알 수 있게 짓는다 (예: `docs/2026-09-11-kepco-plate-format.md`).
